# LumaDrop Optical 0.5.2-luma.7

The zoom limit went from 4x to 10x. Phones that cannot provide that much hardware zoom still fall back safely instead of breaking the camera.

It's an archived debug build. The current version is `luma.9`.
