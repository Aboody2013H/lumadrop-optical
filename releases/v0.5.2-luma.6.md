# LumaDrop Optical 0.5.2-luma.6

Adds a mobile rear/front camera switch and translated camera-control labels while retaining the receiver zoom behavior from `luma.5`.

Archived debug-signed development build; prefer `luma.9`. See the repository changelog for the historical-source note.
