# LumaDrop Optical 0.5.2-luma.6

Added the button for switching between the rear and front cameras, along with translated labels for the new control. It keeps the zoom work from `luma.5` too.

It's an archived debug build. The current version is `luma.9`.
