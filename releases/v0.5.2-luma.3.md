# LumaDrop Optical 0.5.2-luma.3

Some non-image files were unreliable on older WebKit-based devices. This version added a compression fallback for browsers without `CompressionStream`, which made arbitrary-file sending behave properly there too.

It's an archived debug build. Grab `luma.9` unless you're following the development history.
