# LumaDrop Optical 0.5.2-luma.3

Improves arbitrary-file sending on older WebKit-based devices by providing a compression fallback when the browser `CompressionStream` API is unavailable.

Archived debug-signed development build; prefer `luma.9`. See the repository changelog for the historical-source note.
