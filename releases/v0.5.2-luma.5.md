# LumaDrop Optical 0.5.2-luma.5

The first zoom build. You can use the slider, pinch on a touchscreen, or scroll the mouse wheel. The decoder also scans the zoomed crop, so it actually helps reception instead of only enlarging the preview.

It's an archived debug build. The current version is `luma.9`.
