# LumaDrop Optical 0.5.2-luma.5

Adds receiver zoom with a visible slider, touchscreen pinch gestures, mouse-wheel control, and decoder-aware center cropping so the scanner processes the zoomed image.

Archived debug-signed development build; prefer `luma.9`. See the repository changelog for the historical-source note.
