# LumaDrop Optical 0.5.2-luma.2

This build fixed the ugly dark error page from `luma.1`. Send and Receive now stay inside the app's bundled pages, so both sides work offline like they were supposed to.

It's an archived debug build. `luma.9` is the one to use day to day.
