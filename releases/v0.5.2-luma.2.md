# LumaDrop Optical 0.5.2-luma.2

Fixes offline Send and Receive routing inside Android WebView. Navigation now remains on the bundled secure app-assets origin instead of producing the dark `ERR_INVALID_RESPONSE` page.

Archived debug-signed development build; prefer `luma.9`. See the repository changelog for the historical-source note.
