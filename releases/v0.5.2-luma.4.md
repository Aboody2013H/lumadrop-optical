# LumaDrop Optical 0.5.2-luma.4

Adds adaptive frame sizing for small files and text snippets. One-block payloads no longer need unnecessarily dense maximum-capacity QR symbols, improving small-file readability.

Archived debug-signed development build; prefer `luma.9`. See the repository changelog for the historical-source note.
