# LumaDrop Optical 0.5.2-luma.4

Small text files looked strangely frozen because most of the QR stayed the same between frames. They still transferred, but the code was denser than it needed to be. This build added adaptive frame sizing so small transfers use easier-to-read QR codes.

It's an archived debug build. The current version is `luma.9`.
